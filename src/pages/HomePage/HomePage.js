import css from './welcomeTitle.module.css'

const HomePage = () => {
    return (
        <div className={css.welcomeTitle}>
            <h1>HELLO OKTEN</h1>
        </div>
    );
};

export {HomePage};